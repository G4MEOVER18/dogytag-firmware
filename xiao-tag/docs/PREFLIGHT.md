# Join preflight

Use serial command `preflight` before `join`.

It reports:
- SX1262 radio readiness
- OTAA key parse status
- backend name and backend state
- whether join is currently supported by the selected backend path

Use serial command `keys` to print only the OTAA key-format status.
